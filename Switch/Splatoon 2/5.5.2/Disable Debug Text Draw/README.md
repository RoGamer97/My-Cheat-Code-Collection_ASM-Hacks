## Disable Debug Text Draw

Disables debug text draw. 

Useful if you want to record videos with debug features that draw text on-screen but don't want them visible, for example, a cinematic like video of every player following you or copying you using Debug Marching/Leading.